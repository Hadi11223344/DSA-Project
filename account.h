#pragma once
#include <string>

class Account {
private:
    std::string id;
    std::string title;
    int balance;

public:
    Account(){}
    // Getter and setter methods for Account properties
};